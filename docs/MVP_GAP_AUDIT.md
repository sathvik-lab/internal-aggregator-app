# MVP gap audit — `PRD_TRACEABILITY.md` **Partial** rows

Read-only pass: opened referenced paths under `src/` (and `navigationConfig` / rules where cited). Compared **trace doc notes** to **observed behavior**. Use this in a PR description or planning; update `PRD_TRACEABILITY.md` separately when you want legend rows to match reality.

| § / area | PRD / trace row (abridged) | What trace doc says | Observed behavior (evidence) | Delta (doc vs code) |
|----------|----------------------------|----------------------|------------------------------|---------------------|
| **6.1** | Phone sign-up / log in | Phone **sign-in** only; **no** phone on `SignupScreen.js` | `SignupScreen.js` uses `signUpUser` + email/password only. Phone flow lives on `PhoneLoginScreen.js` / `auth.js` (`requestPhoneSignInCode`, etc.). | **Matches.** Optional product gap: phone **registration** not on signup screen. |
| **6.1** | Roles Owner / Staff | Role stored; **no** rules/UI split beyond onboarding + Staff **placeholder** | `USER_ROLES` on signup; `useEffectiveRole.js` exposes `isOwner`; `firestore.rules` + `docs/ROLE_MATRIX.md` describe owner vs staff writes; `StaffScreen.js` lists members + **invite codes** (`createInviteCode`, `listBusinessMembers`) — not an empty placeholder. MainNavigator / screens gate some actions by owner. | **Trace row understates implementation.** Staff management is **partial** (no full HR UX), but **not** “placeholder only.” Consider refreshing trace **Notes**. |
| **6.1** | Owner: logs/reports, staff, visibility, export | Staff management **placeholder**; owner has visibility, PDF export, optional secure link | Same as above for **Staff** — invites + member list exist. `ProfileScreen.js` toggles + `userProfile.js` persist visibility. `readinessExport.js` + `InspectionReadinessScreen.js` for PDF/share. `reportLinks.js` → Cloud Functions when `firebaseProjectId` present. | **Staff:** doc **outdated** vs code. **Secure link:** still depends on deployed functions + config — **Partial** still fair. |
| **6.1** | Staff: checklists, incidents, tasks | Incidents + maintenance UIs exist; **owner-only** doc upload / business doc edit/delete / business media log compose | Screens + `incidents.js` / `maintenanceTasks.js` present. ROLE_MATRIX + rules: staff blocked on those writes. Staff can use checklists/incidents/maintenance per matrix. | **Matches** intent; confirm any new screens stay aligned with `ROLE_MATRIX.md`. |
| **6.2** | Truck/business name, type, permits, location | Onboarding enforces minimum profile; field set may not match every PRD label | `OwnerOnboardingScreen.js`: truck type, state/city, food types, business type, compliance areas; saves via `userProfile.js`. Permits live in **documents** flow, not same form. | **Matches** — profile vs permit split; label coverage vs PRD is product polish. |
| **6.3** | Checkbox, notes, photos, timestamp, **attribution** | Attribution/timestamp “depend on save path; verify per component” | `ChecklistScreen.js` passes `userId`, `updatedAt` on updates; `checklistScheduling.js` seeds instances with `userId`, `completedAt: null`. Full “who completed” audit trail not exhaustively verified every sub-component. | **Partial** still fair — worth one explicit pass listing each checklist component write payload. |
| **6.3** | Critical incomplete → score; missed → dashboard | Score uses critical overdue; dashboard wording may differ from “missed” | `complianceScore.js` applies `priority === 'critical'` penalty on overdue. `DashboardScreen.js` uses **overdue** counts/copy (`overdueItems`, “No overdue checklist items”), not necessarily PRD word “missed.” | **Matches** nuance; UX wording vs PRD only. |
| **6.4** | Incidents types/fields/media | List/create/detail + optional media; **assignment/workflow** limited | `incidents.js`: CRUD, filters, storage path for media; payload includes type/severity/status. No rich assignment workflow in service API beyond reporter as `userId`. | **Matches.** |
| **6.4** | Maintenance assign/track | List/create/detail + status; **assignment lifecycle basic** | `maintenanceTasks.js`: `assigneeUserId` on create + filter in `listMaintenanceTasks`; status updates. No multi-step assignment UI verified here. | **Matches.** |
| **6.6** | Inspection mode, date filters, aggregated views | Readiness aggregates snapshot; **not global “mode”** across app | `InspectionReadinessScreen.js`: **local** `inspectionModeEnabled` toggle (larger score UI, hides some chrome) + date range validation + snapshot-driven content. Not a global app-wide mode flag. | **Trace slightly soft** — **mode exists on readiness screen** but is UI-local, not app-global. Optional trace tweak: “local inspection presentation mode.” |
| **6.6** | Shareable report | Native **Share**; **not** hosted multi-tenant viewer | `readinessExport.js` `shareReadinessSummary` uses `Share.share` with plaintext/HTML/PDF paths per platform. | **Matches.** |
| **6.6** | Secure report link | HTTPS CF; needs **extra project id** + **deployed backend** | `reportLinks.js`: `getFunctionsBaseUrl()` from `expo.extra.firebaseProjectId`; POST with ID token to `generateSecureReadinessReport` / revoke. | **Matches** operational dependency. |
| **6.7** | Permits/certs as documents | Model supports expiry/types; **no** separate cert module / **auto** push reminder | `DocumentsScreen.js`, `DocumentDetailScreen.js`, `storage.js`, `documentTypes.js` — expiry on docs. `NOTIFICATIONS_SPIKE.md` already **Missing** for scheduled reminders row. | **Matches** for this row. |
| **6.7** | Status tags Active / Expiring / Expired | Surfaced; wording may differ | `documentExpiryStatus.js`: labels **Active**, **Expiring soon**, **Expired** on list/detail/card. | **Near-match** — “Expiring soon” vs PRD “Expiring”; acceptable **Partial** for wording only. |
| **6.8** | Public visibility / consent | Defaults private; **no** public read surface today | `ProfileScreen.js` copy states flags are consent for **future** API; `userProfile.js` persists. | **Matches.** |
| **Cross** | Offline-first / sync SLA | Online-first; SDK persistence not full offline-first | `firebase.js`: RN skips IndexedDB persistence; web enables it. No custom offline queue in repo. | **Matches.** |
| **Cross** | Analytics §9 | Named events; full funnel + **production RNFB** depends on build | `logAnalyticsEvent` in `ChecklistScreen.js`, `UploadDocumentModal.js`, `InspectionReadinessScreen.js`, `incidents.js`. `logScreenView` in `AppNavigator.js` for main tab names. `analytics.js`: lazy `require('@react-native-firebase/analytics')` — **no-op in Expo Go** when native module missing. | **Matches.** |
| **Nav** | Deep linking | “Product handling still **partial**” (not in Partial column but same doc) | `AppNavigator.js` passes `linking={DEEP_LINKING_CONFIG}` from `navigationConfig.js` (prefixes + nested paths). `app.config` has Android intent filters / iOS domains. | **Stricter than “config only”** — linking **is** wired; **partial** likely means cold-start/auth edge cases or untested paths, not absence of `linking` prop. |

---

## Summary

- **Stale trace text:** §6.1 **Owner/Staff** row still calls **Staff** “placeholder”; `StaffScreen.js` already implements **member list + invite codes** (owner-only).
- **Soft trace text:** §6.6 **Inspection mode** — screen-level toggle exists; trace says “not global mode” which is accurate; could clarify “local presentation mode on Inspection Readiness.”
- **Nav row:** Deep link **structure** is implemented on `NavigationContainer`; “partial” is probably **QA / auth / universal links**, not missing config object.

*Generated: 2026-04-15 — read-only audit.*
